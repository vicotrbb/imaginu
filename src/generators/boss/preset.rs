//! Archetype presets: fill in sentinel (`-1`) knobs with archetype-specific
//! defaults before the rig is planned. No-op until Task 5.

use crate::recipe::BossParams;

pub fn apply_archetype_preset(_p: &mut BossParams) {}
